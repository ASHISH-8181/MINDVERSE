╔═══════════════════════════════════════════════════════════════════════════╗
║         VISUAL GUIDE - MongoDB Atlas IP Whitelist Fix                    ║
╚═══════════════════════════════════════════════════════════════════════════╝


STEP 1: OPEN MONGODB ATLAS
═══════════════════════════════════════════════════════════════════════════

Browser URL:
   https://cloud.mongodb.com/v2

You should see your dashboard with projects.


STEP 2: SELECT YOUR PROJECT
═══════════════════════════════════════════════════════════════════════════

Look for:
   ┌─────────────────────────┐
   │ mindverse-69a67         │  ← Click here
   │ (Your Project Name)     │
   └─────────────────────────┘

Click on it to open the project.


STEP 3: NAVIGATE TO SECURITY
═══════════════════════════════════════════════════════════════════════════

Left Sidebar Menu:

   ┌────────────────────┐
   │ DEVELOPMENT        │
   │ └─ Databases      │
   │ └─ Collections    │
   │                    │
   │ SECURITY           │ ← Find this section
   │ └─ Network Access │ ← Click here
   │ └─ Database Access│
   │ └─ API Keys       │
   └────────────────────┘


STEP 4: CLICK ADD IP ADDRESS
═══════════════════════════════════════════════════════════════════════════

You'll see:

   IP Whitelist
   ┌──────────────────────────────┐
   │ + Add IP Address             │ ← Click green button
   └──────────────────────────────┘


STEP 5: ADD YOUR CURRENT IP
═══════════════════════════════════════════════════════════════════════════

A dialog will appear with options:

   ┌─────────────────────────────────────────┐
   │ Add IP Address                          │
   │                                         │
   │ ○ Add Current IP Address ← SELECT THIS │
   │   (Your IP: 123.456.789.0)              │
   │                                         │
   │ ○ Allow Access from Anywhere            │
   │   (Less secure option)                  │
   │                                         │
   │ [CANCEL] [CONFIRM]  ← Click CONFIRM    │
   └─────────────────────────────────────────┘

Choose: "Add Current IP Address"
Click: "CONFIRM"


STEP 6: WAIT FOR CHANGES
═══════════════════════════════════════════════════════════════════════════

You'll see:
   
   ⏳ Provisioning... (1-2 minutes)

OR

   ✓ Changes applied

Don't close the page. Wait for it to complete.


STEP 7: YOUR IP IS NOW WHITELISTED
═══════════════════════════════════════════════════════════════════════════

You should see your IP in the list:

   IP WHITELIST
   ┌─────────────────────────────┐
   │ ✓ 123.456.789.0             │ ← Your IP is here
   │   Description: (empty)      │
   │   [Delete]                  │
   └─────────────────────────────┘


STEP 8: START YOUR SERVER
═══════════════════════════════════════════════════════════════════════════

In Terminal:

   $ cd backend
   $ node src/server.js


STEP 9: SUCCESS!
═══════════════════════════════════════════════════════════════════════════

You should see:

   ✓ MongoDB connected successfully
   Connected to: cluster0.ijrurlt.mongodb.net
   ✓ Server running on http://localhost:5000


🎉 YOU'RE DONE!
═══════════════════════════════════════════════════════════════════════════

Your backend is now connected to MongoDB Atlas!

Now you can:
   • Upload files to Firebase
   • Store URLs in MongoDB
   • Test the entire system


🆘 TROUBLESHOOTING
═══════════════════════════════════════════════════════════════════════════

Still getting error?

1. Check you waited long enough (2-3 minutes)
2. Try clicking "Allow Access from Anywhere" (0.0.0.0/0)
3. Delete and re-add your IP address
4. Restart your Node server (Ctrl+C, then run again)
5. Check your MongoDB URI in backend/.env


═══════════════════════════════════════════════════════════════════════════

Need help? Read: MONGODB_WHITELIST_FIX.md (full detailed guide)

═══════════════════════════════════════════════════════════════════════════
